def seive(self,n):
          i=2
          while(i*i<=n):
              if(prime[i]==True):
                   for j in range(i*i,n,i):
                        prime[j]=False
              i+=1
          prime[0]=prime[1]=False

if(n<=1):
        return 0
        seive(self,n)
